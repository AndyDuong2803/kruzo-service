import DocsShell from "@/components/docs/DocsShell";

export const metadata = {
  title: "API Docs",
  description: "Extract structured data from documents into JSON, tables, and reviewable fields.",
};

const DocsPage: React.FC = () => {
  return <DocsShell />;
};

export default DocsPage;
